// Stub for pg module - only used in type imports, never at runtime
export type Pool = any;
export type PoolConfig = any;

export const Pool = undefined as any;
export const PoolConfig = undefined as any;

// Also export as default for CommonJS compatibility
export default {
  Pool: undefined as any,
  PoolConfig: undefined as any,
};
